# UI Showcase

Compile with:

`lumina compile examples/ui-showcase/main.lm --out examples/ui-showcase/main.generated.js --target esm --ast-js`

This example demonstrates the first `@std/ui` styled layer on top of the headless render primitives.
